module.exports = {
  content: [
    './**/*.html',
    './**/*.js',
    // Add any other file types that may contain Tailwind CSS classes
  ],
  darkMode: 'media', // Change `darkMode` to 'media' as suggested in the warning
  theme: {
    extend: {},
  },
  variants: {},
  plugins: [],
}
